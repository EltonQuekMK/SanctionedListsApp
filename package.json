{
  "name": "sanctioned-list-server",
  "version": "1.0.0",
  "description": "A Node.js server project that runs a daily job to scrape a list of companies from a website.",
  "main": "src/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "test": "jest",
    "dev": "nodemon src/server.js",
    "build": "babel src -d build --extensions .ts,.js && uglifyjs-folder build -x .js -eo dist",
    "vercel-build": "babel src -d build --extensions .ts,.js && uglifyjs-folder build -x .js -eo dist"
  },
  "dependencies": {
    "axios": "^1.8.1",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.17.1",
    "fuse.js": "^7.1.0",
    "jsdom": "^26.0.0",
    "jsonschema": "^1.5.0",
    "node-cron": "^2.0.3",
    "pdf-parse": "^1.1.1",
    "uglifyjs-folder": "^3.3.0",
    "xml2js": "^0.6.2",
    "yarn": "^1.22.22"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9"
  },
  "nodemonConfig": {
    "ignore": [
      "*.json"
    ]
  }
}
